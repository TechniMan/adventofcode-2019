#include <string>
#include <iostream>
#include <fstream>
#include <vector>
#include <cmath>

std::string readline(std::istream& inStream) {
	std::string result;
	std::getline(inStream, result);
	return result;
}
